# Frantic #133 — Ausca Media Transcription end-to-end report

- **Scope:** Real paid `media.transcription` run for Frantic bounty #133, claim `556e5c36-c6bf-4c45-a6e1-69f11d6142e3`. I used a genuine 488,328-byte WAV and did not fabricate requests, responses, timings, or findings.
- **Discovery / pinned contract:** Live offer was `media.transcription`, revision `transcription-bytes-r6`, revision digest `sha256:4a9d20f6a8e7cde3a3b92ad951c9e256a6b57ad8309da57eb751660ccafd56fb`. Input/output schema digests were pinned before payment.
- **Artifact commit:** The WAV commit took **4.026 s**. Ausca returned `runx:artifact:sha256:7fbc12ac8f36975c6db4cb42d5d4a72849b0c4af2e072ff9aa56030bf406c280`; its content digest exactly matched local SHA-256 `sha256:28c214064d116e393e06253dd4e44f1a5b104c5c8774de1d9b20e02939410303`.
- **Offer read:** Live offer read took **0.219 s**. The final request used `media_format=wav` and `language_code=en-US`.
- **402 challenge:** Unsigned invocation returned the x402 v2 requirement in **1.534 s**: **0.40 USDC**, Base (`eip155:8453`), USDC contract `0x833589fcd6edb6e08f4c7c32d4f71b54bda02913`, pay-to `0x26572ff23c6c52bfb1a69cb0c9114a8be443b422`.
- **Issue 1 — HTTP 200 is not necessarily success:** An earlier attempt returned HTTP 200 but body `status=refused`, `code=replay_conflict`. A client that only checks HTTP status hides the real failure. The response had no PAYMENT-RESPONSE and a chain check found no outgoing USDC for that attempt.
- **Issue 2 — replay recovery was not obvious:** Reusing the same invocation/idempotency identity with a newly generated EIP-3009 authorization caused `replay_conflict`. After reconciling the old invocation as still `admitted` with no settlement, I generated a fresh idempotency identity instead of blindly resubmitting. The successful invocation was `paid_5ea32dea-c072-440e-abeb-4c7fb25b3212`.
- **Payment:** Fresh prepare was at **2026-09-22T01:48:52.266Z**. The 0.40 USDC transfer confirmed on Base at **01:50:15Z**, about **82.7 s** later, in tx `0xfefe9486fb16633d7ed52a880c189ee58d2509f5f0627090b30eafdd747eaae2` (block 51626234).
- **Async wait:** The provider returned an admitted asynchronous invocation. It reached `succeeded` at **01:50:57.040Z**, about **42.0 s** after chain confirmation.
- **Result validation:** `output.source_digest` exactly matched the committed artifact digest. `output_digest` was `sha256:303767a3038ff710f8283e037ddf971a8557c872d37c02cdc68e0f24c1bf6274`. Two timed segments were returned, so the result was internally consistent with the output schema.
- **Issue 3 — concrete transcription error:** The second spoken sentence used the product name **“Ausca”**, but the returned transcript rendered it as **“OSCA”**. The rest of the short verification clip was recognizable and correctly segmented.
- **Receipt:** Terminal receipt is https://runx.ai/r/0dffa555f4057e550d8c75db22c64df2dca3ad7561ee64cc671804467e0e69a1. It was notarized at **01:51:11.041Z**, says **Published by Ausca**, **Ausca Media Transcription completed**, and declares **0.40 USD**.
- **Issue 4 — receipt does not expose transaction proof:** The public Runx page explicitly says it is hash-only L1 notarization and does not publish wallet addresses or transaction identifiers. That is privacy-preserving, but it means I had to verify the actual Base USDC movement separately rather than from the receipt page alone.
- **Elapsed time:** The clean fresh successful path from prepared challenge to public receipt was about **138.8 s**. Including the earlier replay-conflict/debugging path from initial artifact creation, the full operator experience was about **22m20s**.
- **Concrete change I would make:** Add a short decision table to the Media Transcription SKILL for paid-call responses: even on HTTP 200, parse `body.status`; surface `refused.code/reason` verbatim; for `replay_conflict`, require reconciliation of `GET /v1/invocations/{id}` and settlement state before deciding whether to create a fresh idempotency identity. That would have made this failure recoverable on the first pass instead of appearing as an opaque payment problem.
