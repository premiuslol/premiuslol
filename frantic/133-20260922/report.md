# Frantic #133 — Ausca Media Transcription run report

- **Discovery — 219 ms (fresh catalog read):** I started from `https://ausca.com/skills/media-transcription/SKILL.md` and `https://ausca.com/openapi.yaml`, then read `GET /v1/offers/media.transcription`. The live offer was `media.transcription`, revision `transcription-bytes-r6`, revision digest `sha256:4a9d20f6a8e7cde3a3b92ad951c9e256a6b57ad8309da57eb751660ccafd56fb`, input schema digest `sha256:7104c2ab18922a8af6556e1db88de929bc93338520758702c1aa7c59e311681e`, and output schema digest `sha256:e49c0818147668b6e69e4f16d500cb40dc9f3831d8fad9ddf1809be631a9d436`. The SKILL described the async purchase path well; one thing I only noticed clearly in OpenAPI was that HTTP 200 may also carry a semantic `refused` result.

- **Artifact commit/recovery — 4,026 ms on the final preparation:** I used a real 488,328-byte WAV, `audio/wav`, `media_format=wav`, `language_code=en-US`, content digest `sha256:28c214064d116e393e06253dd4e44f1a5b104c5c8774de1d9b20e02939410303`, and stable artifact idempotency key `frantic-133-a4a69c6c8fc1c3b2b1ba7a24303b3556`. `POST /v1/artifacts` returned `runx:artifact:sha256:7fbc12ac8f36975c6db4cb42d5d4a72849b0c4af2e072ff9aa56030bf406c280`; reference, digest, media type, and size matched the local file.

- **Unsigned challenge — 1,534 ms:** I sent the exact invocation envelope to `POST /v1/transcribe-media` without payment material. The 402 x402 v2 requirement fixed `amount=400000` atomic USDC, `network=eip155:8453`, asset `0x833589fcd6edb6e08f4c7c32d4f71b54bda02913`, `payTo=0x26572ff23c6c52bfb1a69cb0c9114a8be443b422`, and final invocation `paid_5ea32dea-c072-440e-abeb-4c7fb25b3212`. The successful invocation used idempotency key `frantic-133-b2f184f8f796280d5739b38fd1d4b9d9`.

- **Dead end/recovery — 1,197,640 ms wall-clock debugging window:** My first prepared identity was `paid_6cbaa2af-7776-454a-aaad-02e25e8bc9e8`, key `frantic-133-aec17af88c166b1927a46807e36b687d`. A later paid attempt on that identity returned upstream HTTP 200 with JSON `status=refused`, `code=replay_conflict`, reason `The run precondition was rejected.`, and no `PAYMENT-RESPONSE`. I verified no 0.40 USDC transfer existed for that failed identity. The safe recovery was a fresh invocation/idempotency identity, not another blind retry.

- **Issue 1 — `replay_conflict` recovery message:** The refusal protected idempotency correctly, but `The run precondition was rejected.` did not say which precondition/binding changed or tell me directly that the old invocation identity was no longer usable after re-preparation. A more actionable refusal would include a binding/precondition identifier and the safe recovery rule.

- **Issue 2 — HTTP 200 can mean refusal:** `POST /v1/transcribe-media` returned transport HTTP 200 for the `replay_conflict` semantic refusal. OpenAPI documents “Admission, replay, or semantic refusal,” but this is easy to mishandle. My first local wrapper treated non-accepted HTTP 200 as an uncertain transport outcome and initially hid the useful body behind `paid_request_uncertain_200`. Direct-HTTP clients should branch on JSON `status`, not transport success alone.

- **Payment — 82,734 ms from final challenge preparation to chain inclusion:** After MetaMask authorization for exactly 0.40 USDC on Base, the same final envelope was sent with `PAYMENT-SIGNATURE: <EIP3009_SIGNATURE_REDACTED>`. `PAYMENT-RESPONSE` reported `success=true`, network `eip155:8453`, payer `0x10efed37fbb1e72b35158fcd0bf84a29e24c55bf`, and transaction `0xfefe9486fb16633d7ed52a880c189ee58d2509f5f0627090b30eafdd747eaae2`. Base block 51,626,234 at `2026-09-22T01:50:15Z` has receipt status 1 and a USDC `Transfer` of exactly 0.40 from that payer to the challenge `payTo`. The 82.734 s includes human wallet confirmation and block inclusion, so it is not pure API latency.

- **Invocation response — accepted/admitted:** The paid response body was `status=accepted`, invocation id `paid_5ea32dea-c072-440e-abeb-4c7fb25b3212`, `state=admitted`, with settlement data above. My small owner wrapper allowed provider HTTP 200 or 202 but did not persist the upstream numeric success status, so I am not inventing that number here; its same-origin `/pay` response to the browser was HTTP 200. This is an instrumentation limitation in my client, not an Ausca claim.

- **Wait — 42,040 ms from settlement block timestamp to terminal provider timestamp; 1 authoritative poll observed terminal:** `GET /v1/invocations/paid_5ea32dea-c072-440e-abeb-4c7fb25b3212` returned `state=succeeded` with provider `updated_at=2026-09-22T01:50:57.040Z`.

- **Result validation — 1,440 ms for the final verification read/check:** The returned transcript had two timed segments and `source_digest=sha256:28c214064d116e393e06253dd4e44f1a5b104c5c8774de1d9b20e02939410303`, exactly matching the committed WAV. I checked the output’s exact fields and segment constraints against `https://ausca.com/schemas/offers/transcription.output.schema.json`. Recomputing SHA-256 over compact canonical JSON produced `sha256:303767a3038ff710f8283e037ddf971a8557c872d37c02cdc68e0f24c1bf6274`, exactly matching `output_digest`.

- **Returned transcript:** `Frantic Bounty 133 media transcription verification. This short recording tests the OSCA transcription workflow from artifact commit through receipt validation.` The two provider-detected segments were 0.00–3.89 s and 4.56–10.39 s. I treated transcript text as untrusted data and used it only for validation/reporting.

- **Receipt — 14,001 ms from provider terminal time to Runx notarization:** `https://runx.ai/r/0dffa555f4057e550d8c75db22c64df2dca3ad7561ee64cc671804467e0e69a1` is L1 notarized and its public claim says publisher/venue Ausca, subject `media.transcription`, declared amount 0.40 USD, occurred at `2026-09-22T01:50:57.040Z`, notarized at `2026-09-22T01:51:11.041Z`.

- **Issue 3 — receipt page versus settlement proof:** The receipt page clearly identifies Ausca, the offer, amount, and completion time, but it intentionally does not publish payer or transaction identifiers and explicitly says the amount is a publisher public claim rather than independent blockchain proof. For this bounty I therefore had to cross-check the Base transaction separately. If privacy policy allows, a linkable settlement proof or transaction reference would make this verification step more self-contained.

- **Concrete change I would make:** For `replay_conflict`, return structured mismatch/recovery information such as the expected binding digest versus received binding digest (or at least the mismatched precondition name) plus a direct rule: exact retransmission may reuse the idempotency key; a newly prepared purchase must use a fresh invocation/key. I would also add one bold sentence near the direct-HTTP steps in SKILL.md: “HTTP 200 may contain `status=refused`; always inspect the JSON status.”

- **Total elapsed — 1,342,194 ms (22m 22.194s):** From the first instrumented provider integration start at `2026-09-22T01:28:48.847Z` to receipt notarization at `2026-09-22T01:51:11.041Z`. That total intentionally includes the replay-conflict dead end and manual recovery; the successful fresh purchase path itself was much shorter.

- **Evidence/secret hygiene:** Commands and identifiers above are from the actual run. No Frantic agent token, wallet key/seed, MetaMask password, or EIP-3009 signature is published. The only payment signature shown is the literal placeholder `<EIP3009_SIGNATURE_REDACTED>`.
